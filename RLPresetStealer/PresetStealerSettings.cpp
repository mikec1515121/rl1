#include "pch.h"
#include "RLPresetStealer.h"

/*
void RLPresetStealer::RenderSettings() {

    ImGui::TextUnformatted("A really cool plugin");


    if (ImGui::Button("Activate Preset Stealer")) {
        gameWrapper->Execute([this](GameWrapper* gw) {
            cvarManager->executeCommand("PresetStealEnable");
            });
    }
    if (ImGui::IsItemHovered()) {
        ImGui::SetTooltip("Activate Preset Stealer");
    }

}


*/